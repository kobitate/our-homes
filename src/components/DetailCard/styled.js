import styled from 'styled-components'

import { CardTitle, CardSubtitle } from 'reactstrap'

const AddressTitle = styled(CardTitle)`
  font-size: 2em;
`

const AddressSubtitle = styled(CardSubtitle)`
  font-size: 1.3em;
`

export { AddressTitle, AddressSubtitle }
